import { _decorator, Component, Node } from 'cc';
import { ESceneType } from '../../enum/EScene';
import { EUIId } from '../../enum/EUI';
const { ccclass, property } = _decorator;

@ccclass('SceneBase')
export class SceneBase extends Component {

    start() {

    }

    initSceneData(data: any) {

    }
}

